(defun clear()
(interactive)
(replace-string "Nullable" "")
(replace-string "
required" "")
(replace-string "
string <uuid>" " UUID")
(replace-string "
string" " string")
(replace-string "
number <double>" " float64")
(replace-string "
integer <int64>" " int64")
(replace-string "
boolean" " bool")

)
(clear)

// Tips type ID.
// Can be obtained by /api/1/tips_types operation. [required]
id UUID `json:"id"`

// Tips type name. [required]
name string `json:"name"`

// Supported organizations IDs. [required]
// Can be obtained by /api/1/organizations operation.
organizationIds []UUID `json:"organizationIds"




orderServiceTypes
required
Array of strings (iikoTransport.PublicApi.Contracts.OrderTypes.OrderServiceType)
Items Enum: "Common" "DeliveryByCourier" "DeliveryPickUp"
Supported order service types.

paymentTypesIds
required
Array of strings <uuid>
Supported payment types IDs.

"./assets/images"
"/../upload/files"